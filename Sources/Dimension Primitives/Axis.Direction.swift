// Axis.Direction.swift
// Direction along an axis.

public import Axis_Primitive
public import Direction_Primitive

// MARK: - Axis.Direction typealias

extension Axis {
    /// Direction along an axis (dimension-independent typealias onto the `Direction` atom).
    public typealias Direction = Direction_Primitive.Direction
}
